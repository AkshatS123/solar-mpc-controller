"""End-to-end demo entry point. Wired up in Phase 2."""

from __future__ import annotations


def main() -> None:
    raise NotImplementedError(
        "Phase 2: synthetic trace + MPC + plot. Run `uv run pytest -q` for now."
    )


if __name__ == "__main__":
    main()
